<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.1//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.openmind.hacadaptor.dao.UMMapper">
    <select id="getOperatorById" parameterType="String" resultType="String">
        select operator from usermapping where userid=#{id}
    </select>
</mapper>